# RandomNamePicker（多功能随机抽取器）插件开发文档

> 适用版本：V1.1.0（宿主插件化 Stage3 + 插件 UI 槽一期/二期均已验收，2026-09-05）。
> 读者对象：希望在**不改动宿主源码**的前提下，为宿主扩展新能力（新模式、新按钮/菜单项、自绘窗口等）的 Java 开发者。
> 文档依据：`src/com/randomnamepicker/plugin/**`、`src/com/randomnamepicker/mode/**` 源码与 `examples/` 下全部示例工程。

---

## 目录

1. [概述：宿主与插件体系](#1-概述宿主与插件体系)
2. [插件能做什么、不能做什么](#2-插件能做什么不能做什么)
3. [核心机制：加载模型、暂存-提交、原子拒载](#3-核心机制加载模型暂存-提交原子拒载)
4. [开发一个最小插件（含编译打包命令）](#4-开发一个最小插件含编译打包命令)
5. [API 参考](#5-api-参考)
   - 5.1 `Plugin`（插件入口）
   - 5.2 `PluginContext`（上下文 / 注册 API）
   - 5.3 `UiZone`（UI 动作区域）
   - 5.4 `ModeHandler` / `ModeHost`（自定义模式）
   - 5.5 `ModeRegistry`（内置注册表，只读参考）
   - 5.6 服务访问（Manager 族）
   - 5.7 `PluginManager` 只读查询与防御执行
6. [线程模型与 EDT 规约](#6-线程模型与-edt-规约)
7. [模式开发规范（模式 = 一种抽取玩法）](#7-模式开发规范模式--一种抽取玩法)
8. [UI 动作（Zone）开发规范](#8-ui-动作zone开发规范)
9. [日志约定](#9-日志约定)
10. [完整案例拆解：正计时子球插件（CountUpTimerBall）](#10-完整案例拆解正计时子球插件countuptimerball)
11. [常见失败模式与调试（QA 样例对照）](#11-常见失败模式与调试qa-样例对照)
12. [插件开发红线（Checklist）](#12-插件开发红线checklist)
13. [验收与自测清单](#13-验收与自测清单)
14. [附录](#14-附录)

---

## 1. 概述：宿主与插件体系

**宿主**：多功能随机抽取器（`RandomNamePicker`），纯 Java Swing 桌面程序（Windows、中文 UI、无第三方依赖、源码 UTF-8）。包结构 `com.randomnamepicker.*`，主类 `com.randomnamepicker.main.Main`。

- 三种内置玩法（模式）：名字列表模式 / 数字模式 / 座位模式；玩法统一为"滚停式"抽取（`RollingPicker` 引擎：主窗无限滚动，悬浮球 20 次自停）。
- 数据根目录 = **程序工作目录（user.dir）**，运行时自动创建 `data/`（加密持久化 + 三副本自愈）、`log/`、`plugins/`。
- 常驻能力：系统托盘、透明悬浮球（双击即用当前模式抽取）。
- 全局锁概念（`PasswordManager`，默认锁定、验密解锁、Ctrl+L×10 后门）：锁定态下名单导入导出、名单/方案增删等被拦。插件 API **不暴露**密码/锁相关类，插件自己的功能不受锁约束，但也不应绕过锁去修改宿主名单/方案（见红线）。

**插件体系**（Stage3 建成、UI 槽一二期扩充，均为宿主内建，非第三方框架）：

| 机制 | 说明 |
|---|---|
| 部署 | 独立 `.jar` 复制到运行目录 `plugins/`（与 jar/exe 同级，不存在自动创建），**重启程序**后生效 |
| 契约 | `com.randomnamepicker.plugin.Plugin` 接口：`getName()/getVersion()/onLoad(ctx)/onUnload()` |
| 能力 | ① 注册新模式（`registerModeHandler`）② 注册 UI 动作按钮/菜单项（`addUiAction(UiZone,…)`）③ 访问数据/日志/方案/名单服务 ④ 自由实现任意 Swing 逻辑（宿主只对注册项负责） |
| 边界 | 无运行中热插拔；无卸载内置项路径；jar 内**禁止**打包 `com/randomnamepicker/**`（宿主 parent-first 提供 API） |
| 质量保证 | 每个 jar 是一个**原子加载事务**：任何失败 → 整 jar 拒载回滚，不污染 UI/注册表，不影响其它 jar |

仓库内可参考/可运行的交付物：

| 路径 | 内容 |
|---|---|
| `examples/plugin-example/` | 最小完整示例：`ExamplePlugin.java`（模式 + 三类菜单 + 六窗口"注意"按钮）+ `BUILD.txt` + 已编译 jar。当前 `plugins/ExamplePlugin.jar` 即此示例 |
| `examples/plugin-timer/` | 高级完整案例：正计时子球插件（5 个源码文件，自绘窗口 + 全局 AWT 观察 + 自持配置）。当前 `plugins/CountUpTimerBall.jar` 即此插件 |
| `examples/plugin-qa/` | 四个**坏样例** jar + 源码（BadCtor / BadOnLoad / BadNullZone / Conflict），验收"整 jar 原子拒载"用 |
| `examples/plugin-ui-zone-demo/` | 一期演示留档（功能已并入 ExamplePlugin 2.0） |
| `plugins/` | 运行目录，现放置上述两个示例插件（按文件名排序加载） |

> 相关文档（任务书/交付报告，供追溯设计决策）：`Stage3.txt`、`Stage3_交付报告.md`、`插件UI槽一期/二期任务书.md` 与 `插件UI槽一期/二期交付报告.md`、`正计时子球开发方案.md`。`背景主题插件开发方案.md` 已废弃——"背景主题"最终按决策升级为宿主内建（`com.randomnamepicker.theme`），**不是插件**。

---

## 2. 插件能做什么、不能做什么

### 能做的

1. **注册新的抽取模式**（玩法）：实现 `ModeHandler`，经 `ctx.registerModeHandler(handler)` 注册。成功后：
   - 模式显示名出现在主窗"抽取模式"下拉框（内置三项之后，按插件提交序）；
   - 主窗"开始抽取"与悬浮球双击都自动使用该模式（共用同一 Handler 取样）；
   - 模式两个附加按钮（可选）出现在主窗按钮阵第 4/6 格。
2. **注册 UI 动作**：在主窗/系统托盘/悬浮球右键三类菜单，以及主窗/设置/配置名单/方案管理/数字设置/座位设置六个窗口的"插件"按钮区，各挂入一个按钮/菜单项（标题自定，点击回调 `ActionListener`）。动作由宿主在 EDT **防御执行**（异常只记日志、不弹崩溃）。
3. **访问宿主服务**：名单（`NameManager`）、方案（`SchemeManager`）、加密持久化（`DataManager`）、日志（`LogManager`/`ctx.log`）、当前方案与宿主窗口（经 `ModeHost` 形态的 `getModeHost()`）。
4. **实现任意扩展 UI**：宿主不限制插件开新窗口/新监听。参考案例（正计时子球）甚至叠加了一个自绘子窗口跟随宿主悬浮球、安装全局 AWT 监听观察单击、自持 `data/xxx.properties` 配置。

### 不能做的（机制边界）

- **无运行中热插拔**：增删 `plugins/` 下 jar、改注册内容都必须重启程序。
- 不能移除/修改内置模式、内置菜单、内置按钮；插件动作无"卸载后自动消失"路径——卸载只在**程序退出**时发生（`onUnload`）。
- 宿主不暴露：`PasswordManager`、`ConfigManager`、`FloatingBall`、`NamePickerApp`（内部类）；插件拿不到这些类型（`PluginContext` 不提供）。
- 不能自行 `new DataManager` 等文件句柄类并期望与宿主共享状态——`getDataManager()` 应使用宿主自持实例（见 5.6）。
- jar 内禁止打包 `com/randomnamepicker/**` 的任何类（宿主 parent-first 提供；宿主不做强制检查，属**文档约束**，违反将导致类加载冲突/版本错乱）。

---

## 3. 核心机制：加载模型、暂存-提交、原子拒载

### 3.1 加载时序（启动）

```
Main.main (EDT)
 └─ new NamePickerApp()                      // 主窗就绪
 └─ PluginManager.init(mainApp)              // 绑定宿主（NamePickerApp 即 ModeHost）
 └─ 显示主窗、建托盘
 └─ PluginManager.loadAllAsync()             // 后台线程 "plugin-loader"，不阻塞 UI
     ├─ 扫描 user.dir/plugins/*.jar（不存在则创建目录），按文件名排序逐个处理
     ├─ 每个 jar 独立处理（见 3.2）
     └─ 有插件提交成功 → EDT 一次性刷新（主窗"插件"菜单/模式下拉框/按钮区 + 托盘菜单重建）
```

退出：`Main.cleanupAndExit` → `PluginManager.shutdown()` → 对每个已提交插件调 `onUnload()`（逐个 try/catch）→ 关闭全部 `URLClassLoader` → 清空注册。

### 3.2 单个 jar 的加载事务（原子单元）

对每个 `*.jar`：

1. 新建 **parent-first URLClassLoader**（parent = 宿主类加载器）。
2. 遍历 jar 内全部 `.class` 条目（跳过 `module-info.class` 与 `META-INF/versions/`），**逐条 `Class.forName` 预加载**——任一失败（缺依赖、类版本不兼容、链接错误…）→ 整 jar 拒载。
3. 收集满足条件的 `Plugin` 实现类（是 `Plugin` 子类型、非接口、非抽象、**public + 无参构造**）。
4. 逐个：反射无参构造实例化 → `onLoad(ctx)`，**每个插件各自一个 `PerPluginContext` 暂存区**，注册动作只进暂存区、不碰 UI/注册表。
5. 全部成功后进入**提交阶段**：对暂存内容做冲突预检（见 3.4）；通过才整体生效（写入 PluginManager 已提交区），随后 EDT 刷新一次 UI。
6. 上述任一环节失败（含 `addUiAction(null,…)` 抛的 `IllegalArgumentException`）→ **整 jar 原子拒载**：已 `onLoad` 的候选补 `onUnload`、丢弃全部暂存、关闭 loader、记日志 `PLUGIN_LOAD_ERROR`；**继续处理下一个 jar**。

> 要点：失败只影响本 jar。宿主本身、其它好插件、UI 全部不受影响；不会出现"按钮注册了一半"的半状态。

### 3.3 暂存-提交语义（为什么看不到"半成品"）

- `onLoad` 期间调用 `registerModeHandler / addUiAction / addMainMenuAction / addTrayMenuAction / addFloatingBallMenuAction` **只写入本插件的 pending 区**。
- 整个 jar 的候选全部 `onLoad` 成功 → 提交时一次性做冲突预检 → 通过才对外可见（下拉框、菜单、按钮区、托盘）。
- 任一个 jar 内候选失败 → 整 jar pending 丢弃，**无任何残留**。

### 3.4 冲突预检（提交阶段，同步持有锁）

对每个待提交的 `ModeHandler` 校验 `getModeId()` 与 `getDisplayName()`：

- 与**内置**（ModeRegistry 的三项）冲突；
- 与**更早提交的插件**冲突；
- 同 jar 内**候选互撞**（两个候选注册了相同的 modeId 或 displayName）；
- 任一命中 → 整 jar 拒载。

UI 动作标题**不判重**：重复标题（含与内置菜单重名、与其它插件重名）各自独立成项，照常显示。

### 3.5 关键规则速记

| 规则 | 值 |
|---|---|
| 插件目录 | `plugins/`（user.dir 下；`.jar` 后缀，忽略大小写；按文件名排序） |
| 类加载 | parent-first；宿主先加载 `com/randomnamepicker/**` |
| 事务粒度 | 一个 jar |
| 拒载条件 | 类条目加载失败 / 实例化失败 / onLoad 异常 / 提交冲突 / zone=null 缺陷 |
| 可见性 | 提交成功后才可见；提交在 EDT 刷新一次 UI |
| 热插拔 | 无（增删 jar、改注册内容均需重启） |
| 标题判重 | UI 动作标题不判重；模式 modeId/displayName 判重（见 3.4） |
| 退出顺序 | shutdown → 逐插件 onUnload（try/catch）→ 关闭 loader |

---

## 4. 开发一个最小插件（含编译打包命令）

### 4.1 源码（默认包即可，与示例一致；也可自建包名）

`MyPlugin.java`（建议 `-encoding UTF-8`，文件与源码均 UTF-8）：

```java
import com.randomnamepicker.core.LogManager;
import com.randomnamepicker.mode.ModeHandler;
import com.randomnamepicker.mode.ModeHost;
import com.randomnamepicker.plugin.Plugin;
import com.randomnamepicker.plugin.PluginContext;
import com.randomnamepicker.plugin.UiZone;
import java.awt.Component;
import java.awt.event.ActionListener;
import java.util.function.Supplier;
import javax.swing.JOptionPane;

public class MyPlugin implements Plugin {

    @Override
    public String getName() {
        return "MyPlugin";
    }

    @Override
    public String getVersion() {
        return "1.0";
    }

    @Override
    public void onLoad(PluginContext ctx) {
        // 1) 注册一个自定义模式（玩法）
        ctx.registerModeHandler(new MyModeHandler(ctx.getModeHost()));

        // 2) 三类菜单各加一项（旧便捷 API，等价 addUiAction(MAIN_MENU/TRAY_MENU/FLOATING_BALL_MENU,…)）
        ActionListener about = e -> JOptionPane.showMessageDialog(
                (Component) ctx.getModeHost().getOwner(),
                "MyPlugin 1.0 已加载", "关于插件", JOptionPane.INFORMATION_MESSAGE);
        ctx.addMainMenuAction("关于插件", about);
        ctx.addTrayMenuAction("关于插件", about);
        ctx.addFloatingBallMenuAction("关于插件", about);

        // 3) 设置窗"插件"区按钮（UI 槽一期开放 Zone；二期起六窗口均可）
        ctx.addUiAction(UiZone.SETTINGS_WINDOW, "弹个提示", e ->
                JOptionPane.showMessageDialog((Component) ctx.getModeHost().getOwner(),
                        "你好", "提示", JOptionPane.INFORMATION_MESSAGE));

        LogManager.log("MyPlugin 加载成功", "PLUGIN_LOADED");
    }

    @Override
    public void onUnload() {
        // 程序退出前清理（幂等、不抛异常）
        LogManager.log("MyPlugin 卸载", "PLUGIN_UNLOADED");
    }

    /** 自定义模式：固定串候选，无附加按钮。 */
    public static class MyModeHandler extends ModeHandler {
        public MyModeHandler(ModeHost host) {
            super(host);
        }

        @Override
        public String getModeId() {
            return "my_mode";          // 稳定唯一；勿与内置(name_list/number/seat)/其它插件撞
        }

        @Override
        public String getDisplayName() {
            return "我的插件模式";      // 下拉框文本；同样全局唯一
        }

        @Override
        public String canPick() {
            return null;               // null = 可以抽取
        }

        @Override
        public Supplier<String> nextCandidate() {
            return () -> "MyPlugin 结果"; // 每次滚显取一次
        }

        @Override
        public void handleButton1Click() { } // 无附加按钮

        @Override
        public void handleButton2Click() { } // 无附加按钮

        @Override
        public String getButton1Text() {
            return null;               // null/空 → 主窗第 4 格隐藏
        }

        @Override
        public String getButton2Text() {
            return null;               // null/空 → 主窗第 6 格隐藏
        }
    }
}
```

### 4.2 编译

插件只依赖 JDK + 宿主 API，把**宿主类路径**（主工程 `out/` 目录或 `RandomNamePicker.jar`）放在 `-cp` 里即可：

```bat
:: 前提：宿主已构建。主工程 out/ 过期时先运行 tools\build.bat（只重编宿主，不改其源码）
javac -encoding UTF-8 -cp ..\..\out -d build MyPlugin.java

:: 或直接以主 jar 作 API 类路径
javac -encoding UTF-8 -cp ..\..\RandomNamePicker.jar -d build MyPlugin.java
```

- 源码语言级别：宿主按 Java 8+ 语法编写；**插件的 class 版本不得高于运行宿主的 JRE**（示例用本机 JDK 22 编译，见 `examples/plugin-timer/BUILD.txt`）。若发布给低版本 JRE 用户，请用 `javac -source/-target` 或匹配 JDK 编译。
- 插件可含任意数量的辅助类/内部类/资源文件（运行时自身要能全部加载成功）。

### 4.3 打包

```bat
jar cf MyPlugin.jar -C build .
```

插件 jar **不需要** `Main-Class` 清单；**只允许包含插件自身类与资源**，禁止包含 `com/randomnamepicker/**`（宿主 parent-first 提供）。打包后可自查：

```bat
jar tf MyPlugin.jar   :: 不应出现 com/randomnamepicker/ 条目
```

### 4.4 安装 / 卸载 / 验证

```bat
:: 安装：复制到运行目录（与 RandomNamePicker.jar/.exe 同级），重启程序
copy MyPlugin.jar ..\..\plugins\

:: 卸载：删除该 jar 后重启（无热插拔）
del ..\..\plugins\MyPlugin.jar
```

验证点：

- `log/Modifylog.txt` 出现 `PLUGIN_LOADED`（名称 版本）；
- 主窗"插件"菜单出现功能项 + 只读"名称 版本"；模式下拉框尾部出现"我的插件模式"；主窗按钮阵下方/设置窗出现"插件"区与按钮；
- 选中插件模式 → 点"开始抽取"→ 停 → 定格 `MyPlugin 结果`；
- 退出程序 → 日志 `PLUGIN_UNLOADED`。

---

## 5. API 参考

### 5.1 `Plugin`（接口）

```java
public interface Plugin {
    String getName();                 // 插件名（显示于主窗"插件"菜单尾部的只读项）
    String getVersion();              // 版本号（与名字拼成 "名称 版本" 展示）
    void onLoad(PluginContext context);   // 后台线程调用；只做注册 + 调度初始化
    void onUnload();                  // 退出时/整 jar 拒载回滚时调用；做清理，须幂等、不抛异常
}
```

要求：

- 实现类必须 **public、非抽象、public 无参构造**（宿主反射 `getConstructor().newInstance()` 实例化）。
- 一个 jar 可以含多个 `Plugin` 实现类（共享同一 loader、各自独立 context；全部成功才整体提交，互撞判冲突）。
- `onLoad` 在**后台加载线程**执行（勿直接操作 UI）；`onUnload` 可能在退出线程调用（清理资源时自行切 EDT，参考正计时子球插件 `FloatingBallTracker.stop()`）。

### 5.2 `PluginContext`（上下文 / 注册 API）

```java
public interface PluginContext {
    // —— 注册（暂存语义：提交后生效；onLoad 期间不触碰 UI/注册表） ——
    void registerModeHandler(ModeHandler handler);          // null 忽略
    void addUiAction(UiZone zone, String title, ActionListener action);

    // 便捷委托（等价 addUiAction 对应 Zone）
    default void addMainMenuAction(String title, ActionListener action) { … }
    default void addTrayMenuAction(String title, ActionListener action) { … }
    default void addFloatingBallMenuAction(String title, ActionListener action) { … }

    // —— 服务访问 ——
    NameManager getNameManager();
    SchemeManager getSchemeManager();
    DataManager getDataManager();
    LogManager getLogManager();
    ModeHost getModeHost();          // 宿主（NamePickerApp 实现）以 ModeHost 接口形态给出
    void log(String detail, String opCode);   // 等价 LogManager.log
}
```

`addUiAction` 严格失败语义（D8）：

- `zone == null` → 抛 `IllegalArgumentException`（插件缺陷）→ 宿主整 jar 拒载并记 `PLUGIN_LOAD_ERROR`；
- `title` 为 null / trim 后为空，或 `action` 为 null → **忽略**（不注册、不报错）；
- 标题**不判重**：重复调用各自独立成项。

### 5.3 `UiZone`（UI 动作区域）

```java
public enum UiZone {
    MAIN_MENU, TRAY_MENU, FLOATING_BALL_MENU,   // 三类菜单（旧 API 委托对象）
    SETTINGS_WINDOW,        // 设置窗"插件"面板（一期试点）
    MAIN_WINDOW,            // 主窗按钮区（二期：3×2 按钮阵下方，两列向下）
    CONFIG_WINDOW,          // 配置名单对话框按钮区（二期）
    SCHEME_MANAGER_WINDOW,  // 方案管理对话框按钮区（二期）
    NUMBER_PICKER,          // 数字设置对话框按钮区（二期）
    SEAT_PICKER             // 座位设置对话框按钮区（二期）
}
```

各 Zone 的实际落点与渲染规则：

| Zone | 渲染位置（宿主源码） | 展示形态 |
|---|---|---|
| `MAIN_MENU` | 主窗 JMenuBar"插件"菜单（`NamePickerApp.refreshPluginMenu`） | 每个动作一个菜单项；有插件时尾部追加分隔线 + 只读"名称 版本" |
| `TRAY_MENU` | 系统托盘弹出菜单（`Main.buildTrayPopup`，插件加载后整体重建） | 每个动作一个 `MenuItem` |
| `FLOATING_BALL_MENU` | 悬浮球右键菜单（`FloatingBall`，**每次新建悬浮球实例**时读取） | 每个动作一个菜单项 |
| `SETTINGS_WINDOW` | 设置窗内容区最末（`SettingsWindow` + `PluginUiSupport.createZonePanel`） | 带"插件"标题边框的按钮行 |
| `MAIN_WINDOW` | 主窗 3×2 按钮阵下方（`NamePickerApp.refreshPluginButtons`：先"插件"标识 + 分隔线行，其下两列向下按钮区） | 两列按钮区 |
| `CONFIG_WINDOW` | 配置名单对话框底部"插件"面板 | 按钮行 |
| `SCHEME_MANAGER_WINDOW` | 方案管理对话框底部"插件"面板 | 按钮行 |
| `NUMBER_PICKER` | 数字设置对话框底部"插件"面板 | 按钮行 |
| `SEAT_PICKER` | 座位设置对话框底部"插件"面板 | 按钮行 |

规则：

- **空 Zone 零渲染**：某 Zone 无动作时，对应区域整体不出现（`PluginUiSupport.createZonePanel` 返回 null，窗口不渲染），界面与无插件时完全一致。
- 窗口对话框每次打开都是**新实例**，打开时实时读取已提交动作 → 在运行期间提交的插件（启动即完成提交，实际不会有此情况）对已打开窗口不生效；对话框关闭重开后可见。
- 悬浮球右键菜单同理：**插件项需要重新打开悬浮球**才出现（宿主每次新建实例读取，机制如此）。
- 二期已开放六个窗口 Zone + 三个菜单 Zone；新增 Zone = 宿主加一个枚举值 + 一处渲染调用，插件 API 无需改动（未来扩展方向）。

### 5.4 `ModeHandler`（抽象类，自定义模式契约）与 `ModeHost`（宿主接口）

```java
public abstract class ModeHandler {
    protected ModeHost host;
    protected SchemeManager schemeManager;
    protected NameManager nameManager;

    public ModeHandler(ModeHost host) { … }        // 构造即注入宿主与两个 Manager

    public abstract String getModeId();             // 稳定标识，全局唯一
    public abstract String getDisplayName();        // 下拉框显示文本，全局唯一
    public abstract String canPick();               // null=可抽取；非 null=不可抽取原因（作弹窗文案）
    public abstract Supplier<String> nextCandidate(); // 每 tick 提供一个候选结果字符串
    public abstract void handleButton1Click();      // 附加按钮 1
    public abstract void handleButton2Click();      // 附加按钮 2
    public abstract String getButton1Text();        // null/空 → 主窗隐藏第 4 格按钮
    public abstract String getButton2Text();        // null/空 → 主窗隐藏第 6 格按钮
}
```

- 插件在 `onLoad` 里 `new 自己的Handler(ctx.getModeHost())` 后 `ctx.registerModeHandler(...)`（构造需要宿主实例，故宿主在提交/取用时注入——参考 `ModeRegistry` 的工厂模式）。
- 钩子被主窗与悬浮球**共用**：主窗 = `canPick()` 校验 → `RollingPicker`（50ms、无限滚动）逐 tick 调 `nextCandidate().get()`；悬浮球 = `canPick()` 校验 → `RollingPicker(maxTicks=20)` 滚 20 次自停。
- 主窗对插件的 `canPick()` / `nextCandidate()` 调用点做了防御包裹：抛异常只记日志，`canPick` 异常回退文案"插件模式校验异常"，候选异常回退占位"插件模式异常"（每轮只记一次日志）。**插件仍应自行保证不抛**。
- 悬浮球对 `canPick()` 非 null 的处理：内置 modeId 映射现状短文本（"损坏/无范围/无座位"）；插件模式无映射 → 回退显示 `canPick()` 返回值原文。
- 主窗对附加按钮动作也做防御（`handleModeButtonClickSafely`）：异常弹"模式按钮操作异常"提示 + 记日志。
- 插件模式**没有 schemeType**：切换方案不会自动拨动到插件模式（内置联动仅针对内置三项）。
- `ModeHandler` 非接口而是抽象类：直接 `extends` 即可，无需额外实现接口。

`ModeHost`（宿主接口形态，插件经 `ctx.getModeHost()` 获得）：

```java
public interface ModeHost {
    Scheme getCurrentScheme();      // 当前方案对象（下拉框选中项）
    NameManager getNameManager();
    SchemeManager getSchemeManager();
    DataManager getDataManager();
    LogManager getLogManager();
    java.awt.Window getOwner();     // 对话框/文件选择器父窗口（AWT Window）
}
```

- 插件常用来：`getCurrentScheme().getName()` 读当前方案、`getOwner()` 作弹窗父窗、取 Manager 服务。
- 主窗（NamePickerApp）即唯一宿主。注意：`getOwner()` 返回 `java.awt.Window`——这是 AWT 模态 owner 链的一部分，插件**不得假设/改写**其语义，仅作父窗口传入即可。

### 5.5 `ModeRegistry`（内置注册表，只读参考）

```java
public final class ModeRegistry {
    static { register(new ModeDefinition("name_list","名字列表模式","name_list",…));
             register(new ModeDefinition("number","数字模式","number",…));
             register(new ModeDefinition("seat","座位模式","seat",…)); }
    public static synchronized void register(ModeDefinition d);    // 重复 modeId/displayName → IllegalStateException
    public static synchronized void unregister(String modeId);
    public static List<String> getDisplayNames();                  // 内置下拉数据源
    public static ModeDefinition getByDisplayName(String name);
    public static ModeDefinition getByModeId(String id);
    public static String getModeIdForSchemeType(String type);
}
```

- **内置三项独占**：`name_list` / `number` / `seat`（displayName：名字列表模式 / 数字模式 / 座位模式）。
- 插件模式**不注册进 ModeRegistry**，由 PluginManager 单独维护，提交后并入下拉框（内置在前、插件在后，均按序）。
- 插件冲突预检会同时比对 ModeRegistry 与已提交插件 → 插件**不要**使用上述内置 id/displayName，也不要与更早提交的插件重复。
- 内置方案类型联动映射 `getModeIdForSchemeType` 仅内置模式使用；插件模式的 schemeType 为 null、不参与联动。

### 5.6 服务访问（Manager 族）

| 服务 | 主要可用方法（供插件参考） | 说明 |
|---|---|---|
| `NameManager` | `getNameList()`、`loadNamesForScheme(scheme)`、`saveNamesForScheme(scheme,List)`、`addNameToScheme/removeNameFromScheme/clearNamesForScheme`、`importFromFile(scheme,File)`、`exportToFile(scheme,File)` | 名单以方案为单位、加密落盘。**锁定态校验在 UI/按钮层**（如内置 Handler），插件直接调用这些方法会绕过锁 —— 请按红线谨慎使用 |
| `SchemeManager` | `getAllSchemes()`、`getNumberRange(scheme)`、`saveNumberRange`、`getSeatConfig`、`saveSeatConfig`、`addScheme/removeScheme` | 方案索引 + 数字范围/座位配置的读写 |
| `DataManager` | 加密持久化底层（名单/数字/座位三类文件 + 备份自愈）；宿主各 Manager 内部使用 | **插件不要自行 `new DataManager`**；`getDataManager()` 返回宿主主窗自持实例（各实例都是文件级操作、指向同一 data/ 根）。自持配置请用 `java.util.Properties` 直写自有文件（见 5.7） |
| `LogManager` / `ctx.log` | `LogManager.log(detail, opCode)`、`readLog()`、`clearLog()` | 追加写 `log/Modifylog.txt`：`[时间]【详情】【操作码】` |
| `ModeHost` | 见 5.4 | 当前方案、父窗口、Manager 访问 |

### 5.7 自持配置/数据的约定

- 宿主配置在 `data/config.properties`（宿主 ConfigManager 管理）；**插件禁止改写它**（ConfigManager 未在 API 中暴露）。
- 插件私有配置推荐 `data/<插件名>.properties` 或 `data/<插件名>/…`（目录不存在先 `mkdirs`）。
- 参考 `examples/plugin-timer/TimerBallConfig.java` 的成熟做法：
  - 默认值 = 代码常量；坏值/缺键回退默认；
  - 合并式读写（只覆盖能解析的键）；写失败仅记日志不影响运行；
  - 颜色等存十六进制串（`parseHexColor/toHex` 工具）。
- 正计时子球插件的图片类资源若需持久化，参考已废弃方案的 D8 决策：**复制进插件自有目录**，不引用用户原路径。

### 5.8 `PluginManager`（进程内单例；查询为只读快照，一般不需要直接用）

```java
PluginManager.getInstance()
    .getPluginInfos()                      // List<PluginInfo{name,version}>
    .getPluginModeDisplayNames()           // 已提交插件模式的显示名（提交序）
    .getPluginModeHandler(displayName)     // 按显示名取 Handler（不存在 null）
    .getUiActions(UiZone zone)             // 某 Zone 的动作只读快照
    .getMainMenuActions()/getTrayMenuActions()/getFloatingBallMenuActions()
    .runMenuActionSafely(pluginName, action, event)  // 宿主内部防御执行动作
    .addUIListener(Runnable)               // 宿主注册 EDT 刷新监听用
```

插件**不应**在 `onLoad` 中直接读 PluginManager 查询自身（尚未提交）；运行期若确需了解宿主侧信息，可经 `ctx.getModeHost()`。`PluginManager.MenuAction` 内部类持有 `title / pluginName / action`，供宿主渲染。

---

## 6. 线程模型与 EDT 规约

| 调用点 | 线程 | 插件应遵守 |
|---|---|---|
| `onLoad(ctx)` | 后台线程 `plugin-loader` | **不得直接操作/创建 Swing UI**；只做注册 + 数据准备 + `SwingUtilities.invokeLater` 调度初始化（参考 `CountUpTimerPlugin.onLoad`）。也可用 `loadAllSync()`（宿主测试/特殊场景）让调用线程执行 |
| 提交后刷新 | EDT（`SwingUtilities.invokeLater`） | 宿主自动做；插件无需关心 |
| UI 动作回调（按钮/菜单点击） | EDT | 宿主用 `runMenuActionSafely` 包裹（try/catch 记日志），插件可直接写 UI；如内部有线程则自行 `invokeLater` 回 EDT |
| 模式按钮点击 `handleButton1Click/2Click` | EDT（主窗按钮事件） | 同上 |
| `canPick()` / `nextCandidate().get()` | EDT（主窗与悬浮球的 RollingPicker 都是 `javax.swing.Timer`） | 保持轻量、快速、幂等；**不要做长任务/弹阻塞对话框/开新线程** |
| `onUnload()` | 退出线程（`PluginManager.shutdown`） | 清理监听/Timer/窗口；涉及 Swing 的销毁自行切 EDT（`invokeLater`/`invokeAndWait`）；必须幂等且不抛（宿主会 catch，但插件应自行兜底） |
| 插件自身开的线程/Timer | 插件负责 | 全部要在 onUnload 停止 |

> 开发插件的通用自防护：定义 `runOnEdt(Runnable)`（非 EDT 时 `invokeLater`），并在 onUnload 置一个 `volatile boolean inactive`，所有异步任务先查它（完整范例见 `CountUpTimerPlugin`）。

---

## 7. 模式开发规范（模式 = 一种抽取玩法）

以模式方式扩展，能让你的能力同时获得：主窗滚停抽取、悬浮球双击抽取、方案上下文、抽取结果日志（宿主按 `方案-模式显示名=结果` 记日志）——不用自己实现任何抽取 UI。

1. **id/显示名全局唯一**：`getModeId()` 建议形如 `厂商/插件前缀_mode`（如 `example_plugin`）；`getDisplayName()` 建议带插件前缀（如"XX 模式"），避免与内置/其它插件冲突被整 jar 拒载。
2. **`canPick()` 语义**：返回 null = 可以开始抽取；返回非 null = 不可抽取原因文案，主窗直接弹该文案并复位、悬浮球显示之。典型实现：在每次抽取开始时快照数据（如内置 Handler 的 cachedNames/cachedRange），缺数据返回提示串。
3. **`nextCandidate()` 语义**：每次滚显取一次候选。正常流程宿主总会先调 `canPick()`，但插件仍应写兜底（无快照时自行重载，参考 `NameListModeHandler`），返回空串兜底即可，**不要返回 null**。
4. **候选应为纯字符串**：主窗直接写大标签，悬浮球会做格式化/截断（>5 字截断等），两处显示规则不同但都接收字符串。
5. **附加按钮**：`getButton1Text()/getButton2Text()` 返回非空文本 → 按钮出现在主窗第 4/6 格；null/空 → 隐藏。点击回调宿主已防御包裹。
6. **弹窗父窗口**：一律用 `host.getOwner()`（ModeHost 的 Window）作 JOptionPane/JFileChooser 父窗。
7. **读取数据**：经 `nameManager`/`schemeManager`（构造时已注入）读**当前方案**（`host.getCurrentScheme()`）的数据。
8. **不要**在 `canPick`/`nextCandidate` 里做 UI 或长阻塞；抽取期间数据中途变更不影响本局（快照语义与内置一致）。

---

## 8. UI 动作（Zone）开发规范

1. 按功能语义选择 Zone：
   - 只与某窗口相关的操作 → 该窗口 Zone（如"计时子球设置" → `SETTINGS_WINDOW`）；
   - 全局性操作 → 菜单 Zone（主窗/托盘/悬浮球右键）；
   - 常用动作希望常驻主窗 → `MAIN_WINDOW`（按钮区在主功能按钮下方，空间有限，克制使用）。
2. **ActionListener 回调运行在 EDT**（宿主 `runMenuActionSafely` 防御包裹），可直接开模态对话框。
3. 模态对话框父窗口获取：
   - 按钮事件源就在窗口内时：`SwingUtilities.getWindowAncestor((Component) e.getSource())`（`CountUpTimerPlugin.ownerOf`）；
   - 兜底：`ctx.getModeHost().getOwner()`。
4. 回调内做任何可能耗时的工作请放到后台线程，避免卡住 EDT。
5. 菜单动作与按钮动作**共用同一 ActionListener 即可**（如正计时子球把"冒出/收回"同时注册到三类菜单 + 悬浮球单击）。
6. 保持标题简洁、可辨识；标题重复虽不报错，但多个同名项会并列显示，影响体验。
7. 悬浮球右键菜单项在**悬浮球下一次新建实例**时生效——测试时先关掉再打开悬浮球。

---

## 9. 日志约定

- 入口：`LogManager.log(详情, 操作码)` 或 `ctx.log(...)`；落盘 `log/Modifylog.txt`，格式 `[yyyy-MM-dd HH:mm:ss]【详情】【操作码】`（UTF-8 追加）。
- 宿主管理码（插件不应仿冒）：`PLUGIN_LOADED`（加载成功）、`PLUGIN_UNLOADED`（卸载）、`PLUGIN_LOAD_ERROR`（拒载及各类插件异常归属）。
- 插件自定义码建议：英文大写 + 下划线、带插件语义前缀，如 `DEMO_BUTTON_CLICKED`（ExamplePlugin）、`TIMER_START/TIMER_PAUSE/TIMER_RESUME/TIMER_RESET/TIMER_BALL_SHOW/TIMER_BALL_HIDE`（正计时子球）。
- 细节日志"详情"字段自由（中文可），但保持单行、简洁，方便人工排查。

---

## 10. 完整案例拆解：正计时子球插件（CountUpTimerBall）

源码：`examples/plugin-timer/`（5 个 .java + `BUILD.txt` + 已编译 jar），设计书《正计时子球开发方案.md》。它是当前**能力最完整**的插件范本，演示了"宿主零改动"能做多深。

| 文件 | 职责 | 可借鉴点 |
|---|---|---|
| `CountUpTimerPlugin.java` | 插件入口：注册设置窗按钮 + 三类菜单 + EDT 初始化调度 + onUnload 清理 | onLoad 只注册与 `invokeLater`；`inactive` 标志防竞态；`runOnEdt` 工具；按钮/菜单/对话框完整链路 |
| `FloatingBallTracker.java` | 控制器：全局 AWT 观察监听（通路 A）+ 悬浮球几何轮询（通路 B，`Window.getWindows()` 探测）+ 单击/双击/拖动判定 | 宿主无钩子时用"观察式监听 + 轮询"扩展宿主交互而不侵入宿主；监听只观察不拦截；所有方法 EDT 规约；start/stop 对称清理 |
| `TimerBall.java` | 计时子球窗口：自绘圆形 + 正计时状态机 | 自绘 `JWindow` 透明子窗 |
| `TimerBallConfig.java` | 外观配置模型：读写插件自持 `data/timerball_plugin.properties` | 默认值常量、合并式读写、坏值回退、十六进制颜色工具 |
| `TimerBallSettingsDialog.java` | "计时子球设置"对话框（尺寸模式/固定直径/三态配色/间距/恢复默认，即时生效） | 模态设置对话框 + 应用回调 |

要点回顾：

- 与宿主的所有集成点只有官方 API：`addUiAction(UiZone.SETTINGS_WINDOW,…)` + 三个菜单 API + `ctx.getModeHost().getOwner()` + `LogManager`；**不 import 任何宿主窗口内部类**。
- 悬浮球交互（单击冒出子球、双击抽取保持原样）通过全局 AWT 监听 + 几何轮询实现——这是"宿主无官方槽时的插件扩展路线"的参考。
- `onUnload` 要停轮询 Timer、卸载 AWT 监听、销毁子球窗口（`FloatingBallTracker.stop()` 用 `invokeAndWait` 切 EDT 处置），并兼容"整 jar 拒载回滚"路径（onUnload 在拒载时也会被补调）。
- 编译：宿主 `out/` 作 classpath（先跑 `tools\build.bat` 保证最新），`javac -encoding UTF-8 -cp ../../out -d build *.java`，再 `jar cf CountUpTimerBall.jar -C build .`。详见 `BUILD.txt`。

---

## 11. 常见失败模式与调试（QA 样例对照）

`examples/plugin-qa/` 提供四个坏样例（源码 + 已编译 jar），用于验证"整 jar 原子拒载"：

| 样例 | 缺陷 | 宿主反应 | 日志 |
|---|---|---|---|
| `BadCtorPlugin` | public 无参构造抛异常 | 实例化失败 → 整 jar 拒载 | `PLUGIN_LOAD_ERROR` |
| `BadOnLoadPlugin` | `onLoad` 抛异常 | onLoad 失败 → 整 jar 拒载，同 jar 已 onLoad 候选补 onUnload | `PLUGIN_LOAD_ERROR` |
| `ConflictPlugin` | 注册 modeId=`name_list` / displayName=`名字列表模式`（撞内置） | 提交冲突预检失败 → 整 jar 拒载（连同其菜单项一并丢弃） | `PLUGIN_LOAD_ERROR` |
| `BadNullZonePlugin` | `addUiAction(null,…)` | zone=null 插件缺陷 → 抛 IllegalArgumentException → 整 jar 拒载 | `PLUGIN_LOAD_ERROR` |

调试速查：

| 现象 | 原因排查 |
|---|---|
| 启动日志没有 `PLUGIN_LOADED`，只见 `PLUGIN_LOAD_ERROR: xxx.jar: …` | 看异常正文：类条目加载失败（缺依赖/类版本不兼容）、实例化失败（构造抛异常）、onLoad 异常（你的 onLoad 抛了）、提交失败（冲突/注册信息异常） |
| jar 放在 plugins/ 但完全没被扫到 | 后缀是否 `.jar`（大小写不敏感）；是否在**运行目录**的 `plugins/`（user.dir），不是源码目录 |
| 改了代码/加了按钮重启后没变化 | 无热插拔：确认宿主进程已完全退出再启动；悬浮球右键项需重新打开悬浮球 |
| 模式下拉框没有你的模式 | 看日志是否拒载；检查 modeId/displayName 是否撞内置或已提交插件；jar 内是否多个候选互撞 |
| 点击你的按钮没反应但也没报错 | 空 title/action 会被忽略（不注册）；动作在 EDT 由宿主防御执行，抛异常只进日志，看 `log/Modifylog.txt` |
| `java.lang.UnsupportedClassVersionError` | 插件 class 版本高于运行宿主的 JRE；用更低/匹配 JDK 编译（可加 `-source/-target`） |
| 模式可选但抽取定格空/异常占位 | `nextCandidate()` 抛异常（宿主回退"插件模式异常"）或返回 null/空串；检查 canPick 是否先于候选做了数据快照 |

---

## 12. 插件开发红线（Checklist）

打包与命名：

- [ ] jar 内**不打包任何 `com/randomnamepicker/**` 类**（含内部类编译产物）；API 由宿主 parent-first 提供。
- [ ] 插件实现类 public + 无参构造；`getName()/getVersion()` 返回稳定值。
- [ ] 模式 `getModeId()`/`getDisplayName()` 全局唯一（内置三项 + 所有已提交插件）；UI 动作标题建议唯一。
- [ ] 文件编码 UTF-8；源语言级别 ≤ 宿主 JRE 可运行范围。

生命周期与线程：

- [ ] `onLoad` 在后台线程：只注册 + 调度（`invokeLater`），不直接碰 Swing。
- [ ] `onUnload` 幂等、不抛异常，清干净：停 Timer/线程、卸载 AWT/组件监听、dispose 自建窗口；兼容整 jar 拒载回滚（会被补调）。
- [ ] 自建后台任务一律检查 `inactive` 标志，任务结束于 onUnload。
- [ ] 需要开对话框/操作 UI 的动作确认在 EDT（宿主回调本身已是 EDT）。

数据与安全：

- [ ] 用 `ctx.getModeHost().getOwner()` 当父窗口；不假设其具体类型。
- [ ] 只经 API 拿宿主服务（`getNameManager/getSchemeManager/getDataManager`），不自行 new 数据类。
- [ ] 插件私有配置写 `data/<插件名>...`；**不改** `data/config.properties`。
- [ ] 谨慎对待锁定态语义：锁定是宿主 UI 层拦截（名单导入/导出、名单/方案增删）。插件直接调 Manager 方法可绕过锁——除非明确需求，否则在涉及宿主名单/方案变更的入口自行尊重锁或提示解锁。
- [ ] 不删改宿主 `data/backup/` 与 `log/` 下的伪装名备份文件（削弱自愈能力）。

健壮性：

- [ ] `canPick()`/`nextCandidate()` 不抛、不阻塞、返回 null/空串兜底（宿主防御只是最后防线）。
- [ ] 依赖宿主内部类/私有字段 = 破坏版本兼容承诺；只依赖本文档列出的 API。

---

## 13. 验收与自测清单

对一个新插件，建议人工验收：

1. **干净基线**：删除插件 jar 重启 → 界面与无插件完全一致（无残留半状态）。
2. **安装**：放 jar 重启 → 日志 `PLUGIN_LOADED`；主窗"插件"菜单（功能项 + 只读"名称 版本"）、模式下拉框、托盘项、对应 Zone 按钮区按预期出现。
3. **模式功能**：选中插件模式 → 开始/停止 → 定格内容正确；日志 `方案-模式显示名=结果`；无附加按钮时第 4/6 格隐藏；插件模式不随方案类型联动。
4. **悬浮球**：打开悬浮球 → 右键菜单含插件项；双击按插件模式抽取（20 次自停）。
5. **设置窗等 Zone 按钮**：打开对应窗口出现"插件"面板按钮，点击正常。
6. **异常隔离**：把 `examples/plugin-qa/` 四个坏 jar 放进 plugins/ 重启 → 只见 `PLUGIN_LOAD_ERROR`×N，好插件照常 `PLUGIN_LOADED`，无残留 UI。
7. **卸载**：退出程序 → 日志 `PLUGIN_UNLOADED`；删除 jar 重启 → 回到干净基线。
8. **宿主零改动复核**：`git diff -- src/`（或对照备份目录）无变化。

自动化参考：宿主验收采用 UI 自动化驱动无插件/混合场景断言（见 README §0 Stage3 验收摘要），可沿用同一脚本思路扩展你自己的插件场景。

---

## 14. 附录

### 14.1 术语表

| 术语 | 含义 |
|---|---|
| 宿主 | 多功能随机抽取器主程序（RandomNamePicker.jar） |
| 插件 / 插件 jar | 实现 `Plugin` 并打包为独立 jar 的扩展单元，部署于 `plugins/` |
| 候选（candidate） | 一个 jar 中实现 `Plugin` 接口的类 |
| 模式（Mode / ModeHandler） | 一种抽取玩法（内置：名字列表/数字/座位） |
| Zone | UI 动作注册区域（菜单或窗口按钮区），见 `UiZone` |
| 暂存（pending） | onLoad 期间插件注册内容的存放处，提交前不可见 |
| 提交（commit） | jar 内全部候选 onLoad 成功后，冲突预检通过、注册内容生效 |
| 拒载 / 回滚 | jar 级原子失败：丢弃暂存、补 onUnload、关闭 loader、记 `PLUGIN_LOAD_ERROR` |
| EDT | Swing 事件派发线程（UI 线程） |
| UI 槽 | 宿主为插件 UI 动作预置的渲染位（窗口"插件"按钮区 / 菜单区） |

### 14.2 宿主相关源码速查（供对照，勿 import 内部类）

| 文件 | 与插件相关职责 |
|---|---|
| `plugin/Plugin.java` | 插件入口接口 |
| `plugin/PluginContext.java` | 注册/服务 API（含 default 菜单便捷方法） |
| `plugin/PluginManager.java` | 单例管理器：加载事务、暂存提交、冲突预检、查询、shutdown |
| `plugin/UiZone.java` | UI 区域枚举 |
| `ui/PluginUiSupport.java` | 宿主渲染帮助（`createZonePanel`） |
| `mode/ModeHandler.java` / `ModeHost.java` / `ModeRegistry.java` | 模式契约 / 宿主接口 / 内置注册表 |
| `main/Main.java` | 启动时序、托盘重建、`cleanupAndExit` → 插件 shutdown |
| `ui/NamePickerApp.java` | 主窗 = ModeHost；插件菜单/下拉框/按钮区刷新 |

### 14.3 已归档/废弃参考

- `背景主题插件开发方案.md`：废弃（背景主题升级为宿主内建 `com.randomnamepicker.theme`）；其 D7/D8 的"插件自有数据目录、复制图片资源"等约定仍可作扩展插件的设计参考。
- `examples/plugin-ui-zone-demo/`：一期 Zone 演示源码留档（功能已并入 ExamplePlugin 2.0）。
- 规划中（Stage 4+）：jlink 免安装打包、插件运行中热插拔——当前机制明确**不提供**，勿按热插拔设计插件。
