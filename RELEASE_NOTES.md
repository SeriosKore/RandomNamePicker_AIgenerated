# GitHub Release 说明（v1.1.0）

> 以下内容可直接粘贴到 GitHub Release 的 "Describe this release" 编辑框。
> 标题（Release title）建议填写：`v1.1.0 名单修改日志 · 开机自启增强 · 多人点名 · 插件生态 · 免安装版`

---

## v1.1.0 — 名单修改日志 · 开机自启增强 · 多人点名 · 插件生态 · 免安装版

本次更新在原有「名字列表 / 数字 / 座位」三模式随机抽取基础上新增四大能力，原有功能（悬浮球、系统托盘、方案管理、加密存储、密码锁定等）全部保持可用。

### ✨ 新增功能

**1. 名单修改日志**

- 名单发生新增 / 删除 / 修改时，自动记录到程序运行目录下的 `modification_log.txt`（文件不存在自动创建，**追加写入、不覆盖历史**）；
- 每条记录包含：操作时间（精确到秒）、操作类型（新增/删除/修改）、涉及条目、修改前后内容对比；
- 设置界面新增「查看名单修改日志」入口。

**2. 开机自启（注册表自动修改）**

- 开启时自动写入 `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`，值名 `RandomNamePicker`，值为当前主程序完整路径（路径含空格自动加英文双引号）；
- 关闭时自动删除该注册表项；注册表访问失败给出明确提示；非 Windows 系统自动跳过；
- 设置界面打开时与注册表实际状态自动同步；自动适配免安装包（jpackage）目录结构。

**3. 多人点名**

- 设置界面新增「单次抽取数量」设置项（最小值 1，最大值 = 当前名单总人数），非法值拒绝并提示；名单人数变化导致超限时自动修正并提示；
- 抽取数量为 **1**：悬浮球保持原有单球效果不变；
- 多人抽取结果**自动去重**且不超过名单总人数；主窗口同步展示全部中奖者；
- 悬浮球多球动画由**多悬浮球插件(示例插件)**提供：主球 + 中奖者小球弹出/旋转/浮动、**渐隐尾迹**、**多环防重叠布局**（人数多时自动排多环）、**透明度可调**（插件设置面板）。

**4. 插件生态（不改主程序即可扩展功能）**

- 将插件 JAR（清单含 `Plugin-Class` 属性）放入程序目录 `extensions/` 即自动加载，无需修改主程序；
- 标准扩展点：主窗口「插件」菜单、主窗口插件按钮区、系统托盘菜单、悬浮球右键菜单、设置窗口插件面板、**悬浮球抽取拦截**（`FloatingBallPickHandler`）；
- **UI 篡改接口**：插件可注入自定义菜单与组件、修改主窗口显示文本，或直接获取主窗口实例自由改造界面；
- 插件异常被隔离（失败仅记录日志，不影响主程序）；程序退出时自动卸载；
- 附赠两个插件（源码与构建脚本见 `examples/`，文档见 `docs/`）：
  - **多悬浮球插件(示例插件)**：多人点名多球动画（本版本示例插件）；
  - **屏幕画笔插件**：参考 Seewo 白板悬浮球画笔——全屏透明画布、**原生指针输入（JNA WM_POINTER）**、Catmull-Rom 平滑 + 变宽轮廓**笔锋**（速度/压感自适应、起收笔渐细）、增量渲染局部重绘**流畅不卡手**；**触屏大面积接触自动识别为橡皮**（鼠标右键亦为橡皮）；工具条选色/清屏/退出、Esc 退出。

### 🛠 工程与修复

- 新增 `plugins/package.bat`、`plugins/selfcheck.bat`；`build.bat` 移至项目根目录，双击即可运行；
- 修复 9 项审查问题：空名单双重弹窗、保存失败时日志与数据不一致、diff 语义、构造函数内弹窗、自检脚本通配符等；
- 模型类 `Scheme`/`NumberRange`/`SeatConfig` 提升为 public（插件 API）；新增 `docs/插件接口文档.md`、`docs/插件开发文档.md`；
- README 与 CHANGELOG 全面更新。

### 📦 使用说明（无需安装 Java）

- **免安装版（推荐）**：下载 `RandomNamePicker-v1.1.0-windows-x64.zip`，解压后双击 `RandomNamePicker.exe` 即可运行——**内置 44.5MB 精简运行时（jlink + jpackage 生成），无需预装 Java 环境**；
- 便携版：`RandomNamePicker.jar`（需 Windows + Java 8 及以上）；
- `data/`、`log/`、`modification_log.txt`、`extensions/` 等文件首次运行自动生成，无需其他辅助文件；
- 初始默认密码见 README「7.2 锁定机制」一节，建议首次运行后立即修改。

### 📎 附件校验和（SHA-256）

- `RandomNamePicker-v1.1.0-windows-x64.zip`：`aa24b7f13cb8d651241648566e2034957adcdab478a32afb6efa75eca7574612`
- `RandomNamePicker.jar`：`f664088aa444fbb3b8d2dc92ade746ed8b2fcfd8765c1d48ae26d1aa19778bac`
- `示例名单.txt`：`931a72713bf7d1db128c1d24eb8036ab0200bacf7c00bb414943d5b8c5b456ab`
