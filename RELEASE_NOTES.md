# GitHub Release 说明

> 以下内容可直接粘贴到 GitHub Release 的 "Describe this release" 编辑框。
> 最新版本 v1.1.1；历史版本 v1.1.0 说明见文末。

---

## v1.1.1 — 动画净化 · 插件管理 · 画笔附着悬浮球

### 🔧 修复与增强

**1. 多球抽取子球取消尾迹**

- 移除中奖者小球的粒子尾迹/拖尾渲染，子球出现**干净无残影**；
- 保留弹出、旋转、浮动与多环防重叠等动画效果。

**2. 插件未安装时隐藏对应设置项**

- “单次抽取数量”仅在“多悬浮球插件”已安装且**启用**时显示；否则置灰并提示“未安装或未启用多球抽取插件”；
- 插件禁用时抽取自动按单人处理；启用状态持久化（`pluginEnabled.<插件标识>`）。

**3. 画笔插件改进（附着悬浮球）**

- 画笔入口**附着在悬浮球上**：铅笔图标随球移动，点击展开/收起工具条（颜色、开始/停止画笔、清屏、收起），不影响悬浮球拖拽与点名；
- **修复鼠标绘制**：新增传统鼠标消息处理，鼠标左键按下/拖动/释放可正常书写，右键为橡皮；
- 全屏透明画布置顶绘制，悬浮球与工具条保持在上层，绘制期间悬浮球仍可拖拽。

**4. 新增插件管理按钮与界面**

- 主窗口“插件”菜单与悬浮球右键菜单新增“插件管理”；
- 可查看插件列表（名称/版本/状态：启用中/已禁用/未安装）、**启用/禁用**（即时生效，设置选项联动显示/隐藏）、**卸载**（删除 JAR）、**安装**（选择 JAR，安装即启用）。

### 📦 使用说明

- **免安装版（推荐）**：下载 `RandomNamePicker-v1.1.0-windows-x64.zip`，解压双击 `RandomNamePicker.exe` 运行，**无需安装 Java**；
- 便携版：`RandomNamePicker.jar`（需 Windows + Java 8+）；
- 插件放在程序目录 `extensions/` 即自动加载。

### 📎 附件校验和（SHA-256）

- `RandomNamePicker-v1.1.0-windows-x64.zip`：`3168f71901fe14097a8188dc072384c0387023f6de7f3063fbfba7c9e900a293`
- `RandomNamePicker.jar`：`0e60d2461f9dc7d2785a077b84b3574d37f7d1ec15b7613e446e0a3f4fd78e1d`
- 示例名单（附件显示名 `default.txt`，内容即 `示例名单.txt`）：`931a72713bf7d1db128c1d24eb8036ab0200bacf7c00bb414943d5b8c5b456ab`

---

## v1.1.0（历史版本说明）

### ✨ 新增功能

**1. 名单修改日志**

- 名单发生新增 / 删除 / 修改时，自动记录到程序运行目录下的 `modification_log.txt`（文件不存在自动创建，**追加写入、不覆盖历史**）；
- 每条记录包含：操作时间（精确到秒）、操作类型（新增/删除/修改）、涉及条目、修改前后内容对比；
- 设置界面新增「查看名单修改日志」入口。

**2. 开机自启（注册表自动修改）**

- 开启时自动写入 `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`，值名 `RandomNamePicker`，值为当前主程序完整路径（路径含空格自动加英文双引号）；
- 关闭时自动删除该注册表项；注册表访问失败给出明确提示；非 Windows 系统自动跳过；
- 设置界面打开时与注册表实际状态自动同步；自动适配免安装包（jpackage）目录结构。

**3. 多人点名**

- 设置界面新增「单次抽取数量」设置项（最小值 1，最大值 = 当前名单总人数），非法值拒绝并提示；名单人数变化导致超限时自动修正并提示；
- 抽取数量为 **1**：悬浮球保持原有单球效果不变；
- 多人抽取结果**自动去重**且不超过名单总人数；主窗口同步展示全部中奖者；
- 悬浮球多球动画由**多悬浮球插件(示例插件)**提供：主球 + 中奖者小球弹出/旋转/浮动、多环防重叠布局、透明度可调。

**4. 插件生态（不改主程序即可扩展功能）**

- 将插件 JAR（清单含 `Plugin-Class` 属性）放入程序目录 `extensions/` 即自动加载，无需修改主程序；
- 标准扩展点：主窗口「插件」菜单、主窗口插件按钮区、系统托盘菜单、悬浮球右键菜单、设置窗口插件面板、悬浮球抽取拦截；
- **UI 篡改接口**：插件可注入自定义菜单与组件、修改主窗口显示文本，或直接获取主窗口实例自由改造界面；
- 插件异常被隔离（失败仅记录日志，不影响主程序）；程序退出时自动卸载；
- 附赠插件（源码与构建脚本见 `examples/`，文档见 `docs/`）：多悬浮球插件(示例插件)、屏幕画笔插件。
