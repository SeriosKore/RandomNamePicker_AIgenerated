# GitHub Release 说明（v1.1.0）

> 以下内容可直接粘贴到 GitHub Release 的 "Describe this release" 编辑框。
> 标题（Release title）建议填写：`v1.1.0 名单修改日志 · 开机自启增强 · 多人点名`

---

## v1.1.0 — 名单修改日志 · 开机自启增强 · 多人点名

本次更新在原有「名字列表 / 数字 / 座位」三模式随机抽取基础上新增三大能力，原有功能（悬浮球、系统托盘、方案管理、加密存储、密码锁定等）全部保持可用。

### ✨ 新增功能

**1. 名单修改日志**

- 名单发生新增 / 删除 / 修改时，自动记录到程序运行目录下的 `modification_log.txt`（文件不存在自动创建，**追加写入、不覆盖历史**）；
- 每条记录包含：操作时间（精确到秒）、操作类型（新增/删除/修改）、涉及条目、修改前后内容对比；
- 设置界面新增「查看名单修改日志」入口。

**2. 开机自启（注册表自动修改）**

- 开启时自动写入 `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`，值名 `RandomNamePicker`，值为当前主程序完整路径（路径含空格自动加英文双引号）；
- 关闭时自动删除该注册表项；注册表访问失败给出明确提示；非 Windows 系统自动跳过；
- 设置界面打开时与注册表实际状态自动同步。

**3. 多人点名**

- 设置界面新增「单次抽取数量」设置项（最小值 1，最大值 = 当前名单总人数），非法值（非数字 / 小于 1 / 超上限）拒绝并提示；名单人数变化导致超限时自动修正并提示；
- 抽取数量为 **1**：悬浮球保持原有单球效果不变；
- 抽取数量 **> 1**：悬浮球变为主球体，中奖者小球从主球周围**弹出、旋转、浮动**，每个小球显示对应姓名；
- 多人抽取结果**自动去重**且不超过名单总人数；主窗口同步展示全部中奖者。

### 🛠 工程与修复

- 新增 `plugins/package.bat`（一键编译打包生成 release 目录）、`plugins/selfcheck.bat`（临时目录自动自检）；
- 修复 9 项审查问题：空名单双重弹窗、保存失败时日志与数据不一致、diff 语义（重复条目 / 误判修改）、构造函数内弹窗、自检脚本通配符等；
- README 与 CHANGELOG 全面更新。

### 📦 使用说明

- 下载 `RandomNamePicker.jar`（主程序）与 `示例名单.txt`（可选示例）到**同一目录**，双击运行即可（需 Windows + Java 8 及以上）；
- `data/`、`log/`、`modification_log.txt` 等文件首次运行自动生成，无需其他辅助文件；
- 初始默认密码见 README「7.2 锁定机制」一节，建议首次运行后立即修改。

### 📎 附件

- `RandomNamePicker.jar` — 主程序
- `示例名单.txt` — 示例名单（可选）

SHA-256 校验和（发布前用以下命令生成并补充到此处）：

```
certutil -hashfile RandomNamePicker.jar SHA256
certutil -hashfile 示例名单.txt SHA256
```
